const Connection = (props) => {
    const connectionStyle = {
        color: props.color,
        
    }

    return ( 
        <div style={connectionStyle}></div>
     );
}
 
export default Connection;